export type Language = {
    name: string;
    icon?: string;
    color: string;
    percentage?: number;
}

export type Platform = {
    name: string;
    icon: string;
    status?: string;
    statusIcon?: string;
    statusColor?: string;
}